"""Pure domain layer: aggregates, VOs, events, state machines, policies.

No I/O, no broker SDK imports, no clock reads (doc 05 preamble).
Phase 1: intentionally empty - populated in the domain build phase.
"""
